import { Placeholder } from "@/components/Page";

export default function NotConnected() {
  return (
    <Placeholder>
      Accounts are not connected yet. This page will work once the store is linked to its database.
    </Placeholder>
  );
}
